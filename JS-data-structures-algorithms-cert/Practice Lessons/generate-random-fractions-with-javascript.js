function randomFraction() {

  // Only change code below this line
  let nonZero = 0;
  while (nonZero === 0) {
    nonZero = Math.random(); //updates nonZero each time until answer is NOT 0.
  }
  return nonZero;

  // Only change code above this line
}
