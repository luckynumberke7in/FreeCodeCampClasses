let a = 8, b = 6;
// Only change code below this line
[b, a] = [a, b]; //seems too simple to be true
//console.log([b,a]=[a,b])
console.log(b); // returns 6
// a & b have been set so let = useless
// as you can see, b is still 6 and a = 8! the order was just changed in the array.