function checkObj(obj, checkProp) {
  // Only change code below this line
  return obj.hasOwnProperty(checkProp)
  //returns answers   ? if match   : if not
    ? obj[checkProp] // returns checked property
    : "Not Found";
  
  // Only change code above this line
}
